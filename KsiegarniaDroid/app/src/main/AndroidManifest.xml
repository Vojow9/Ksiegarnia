<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projekt.ksiegarniadroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">
        <activity android:name=".act.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".act.AuthorsListActivity" />
        <activity android:name=".act.BooksListActivity" />
        <activity android:name=".act.BoughtListActivity" />
        <activity android:name=".act.BasketActivity" />
        <activity android:name=".act.LoginActivity" />
        <activity android:name=".act.RegisterActivity" />
        <activity android:name=".act.SummaryActivity" />
        <activity android:name=".act.BookDetailsInfo" />
    </application>

</manifest>